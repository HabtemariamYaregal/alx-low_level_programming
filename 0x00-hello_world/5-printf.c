#include <stdio.h>
/**
 * main - printswith proper grammar, but the outcome is a piece of art,
 * Return: 0, nothing to return
*/
int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,\n");
return (0);
}
