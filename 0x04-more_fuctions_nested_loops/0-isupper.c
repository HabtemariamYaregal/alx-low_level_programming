#include "main.h"
/**
 * _isupper - check weather a letter is uppercase.
 * @c: a variable to be checked
 * Return: Always 1, if c is upper case.
 */
int _isupper(int c)
{
return (c >= 'A' && c <= 'Z');
}
