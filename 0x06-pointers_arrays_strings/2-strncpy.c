#include <string.h>
#include "main.h"
/**
 * _strncpy - copy from one to another
 * @dest: value that copy
 * @src: value to be copied
 * @n: number os chars
 * Return: dest
*/
char *_strncpy(char *dest, char *src, int n)
{
strncpy(dest, src);
return (dest);
}
