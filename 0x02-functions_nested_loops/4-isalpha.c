#include "main.h"
/**
 * _isalpha - lower case,letter or upper
 * Return: 1, if condition is true
 * 0, otherwise
 * @c: parameter to be checked
*/
int _isalpha(int c)
{
return (c >= 'A' && c <= 'z');
}

