#include "main.h"
/**
 * _islower - lower case,letter or upper
 * Return: 1, if condition is true
	   0, otherwise
 * @c: parameter to be checked 
*/
int _islower(int c)
{
return (c >= 'A' && c <= 'z')
}

