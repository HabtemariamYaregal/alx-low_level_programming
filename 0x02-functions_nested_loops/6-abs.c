#include "main.h"
/**
 * _abs - chech sign
 * Return: n, if number is positive
 * -n, number is negative
 * @n: parameter to be checked
*/
int _abs(int n)
{
if (n > 0)
{
return (n);
}
else
{
return (-n);
}
}
