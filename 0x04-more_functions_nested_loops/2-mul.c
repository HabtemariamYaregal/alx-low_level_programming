#include "main.h"
/**
 * mul - multiplies two integers.
 * @a: variable to be multiply.
 * @b: variable to be multiply.
 * Return: multiple of the numbers
 */
int mul(int a, int b)
{
int c = 0;
c = a * b;
return (c);
}
